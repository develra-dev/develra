name: External contracts

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  develra:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./
        with:
          command: check
          fail-on: probable
