# Synthetic registry API fixtures

These bounded JSON documents exercise the public registry OpenAPI schemas.
Every provider, source, revision, cursor, request ID, and change in this
directory is synthetic. The fixtures are not captured service responses and
are never served by the deployed registry.
