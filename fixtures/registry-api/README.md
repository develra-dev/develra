# Synthetic registry API fixtures

These bounded JSON documents exercise the draft public registry OpenAPI
schemas. Every provider, source, revision, cursor, request ID, and change is
synthetic. The fixtures are not captured service responses and do not imply
that a registry server exists.
