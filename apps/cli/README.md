# Develra CLI

Develra is a local-first external API, SDK, endpoint, webhook, and MCP scanner.
Run `npx develra scan` to create a deterministic `develra.lock`, then
`npx develra check` in CI.

Documentation, source, and contribution guidance live in the
[Develra repository](https://github.com/develra-dev/develra).
